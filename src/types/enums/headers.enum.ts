/* eslint-disable no-unused-vars */
export enum HttpHeaders {
  CorrelationId = "x-correlation-id",
  Authorization = "authorization",
  IpAddress = "ipaddress",
  Agent = "agent",
  ReCaptchaToken = "x-recaptcha-token",
  CacheControl = "Cache-Control",
  OriginStrategy = "x-origin-strategy"
}